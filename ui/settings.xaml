<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="grab -- Settings"
        Width="520" Height="640"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        MinWidth="440" MinHeight="520"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

  <Window.Resources>
    <SolidColorBrush x:Key="Bg"          Color="#111114"/>
    <SolidColorBrush x:Key="BgSoft"      Color="#1A1A1D"/>
    <SolidColorBrush x:Key="BorderSoft"  Color="#26262A"/>
    <SolidColorBrush x:Key="BorderInput" Color="#2A2A2E"/>
    <SolidColorBrush x:Key="Text"        Color="#F5F5F7"/>
    <SolidColorBrush x:Key="TextMuted"   Color="#8E8E93"/>
    <SolidColorBrush x:Key="TextDim"     Color="#5E5E63"/>
    <SolidColorBrush x:Key="Hover"       Color="#1F1F23"/>
    <SolidColorBrush x:Key="SectionLabel" Color="#B8B8BE"/>
    <LinearGradientBrush x:Key="Accent" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#F5A87A" Offset="0"/>
      <GradientStop Color="#E85F62" Offset="1"/>
    </LinearGradientBrush>

    <!-- Section header -->
    <Style x:Key="SectionHeader" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource SectionLabel}"/>
      <Setter Property="FontSize" Value="10"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Margin" Value="0,20,0,10"/>
    </Style>

    <!-- Field label -->
    <Style x:Key="FieldLabel" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Margin" Value="0,0,0,6"/>
    </Style>

    <!-- Text input -->
    <Style x:Key="Input" TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource BgSoft}"/>
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderInput}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="10,7"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="Bd" CornerRadius="6"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <ScrollViewer x:Name="PART_ContentHost"
                            Margin="{TemplateBinding Padding}"
                            VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Ghost / secondary button -->
    <Style x:Key="GhostBtn" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Padding" Value="14,0"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Background" Value="{StaticResource BgSoft}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" CornerRadius="6" Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Hover}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Primary gradient button -->
    <Style x:Key="PrimaryBtn" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" CornerRadius="9" Background="{StaticResource Accent}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Opacity" Value="0.9"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Icon-only button (min/close) -->
    <Style x:Key="IconBtn" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Width" Value="30"/>
      <Setter Property="Height" Value="26"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" CornerRadius="6" Background="Transparent">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Hover}"/>
                <Setter Property="Foreground" Value="{StaticResource Text}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CloseBtn" TargetType="Button" BasedOn="{StaticResource IconBtn}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" CornerRadius="6" Background="Transparent">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#8A2A2A"/>
                <Setter Property="Foreground" Value="White"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Checkbox: custom-styled, minimal -->
    <Style x:Key="ToggleCheck" TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal">
              <Border x:Name="Box" Width="18" Height="18" CornerRadius="4"
                      Background="{StaticResource BgSoft}"
                      BorderBrush="{StaticResource BorderInput}" BorderThickness="1"
                      VerticalAlignment="Center">
                <Path x:Name="Tick" Data="M 3,9 L 7,13 L 15,4" Stroke="White" StrokeThickness="2"
                      Visibility="Collapsed"/>
              </Border>
              <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Box" Property="Background" Value="{StaticResource Accent}"/>
                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Accent}"/>
                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ComboBox: dark styled -->
    <Style x:Key="DarkCombo" TargetType="ComboBox">
      <Setter Property="Background" Value="{StaticResource BgSoft}"/>
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderInput}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Padding" Value="10,4"/>
    </Style>
  </Window.Resources>

  <Border CornerRadius="14"
          Background="{StaticResource Bg}"
          BorderBrush="{StaticResource BorderSoft}"
          BorderThickness="1"
          Margin="14">
    <Border.Effect>
      <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.55"/>
    </Border.Effect>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="44"/>   <!-- title bar -->
        <RowDefinition Height="*"/>    <!-- scroll body -->
        <RowDefinition Height="Auto"/> <!-- action row -->
      </Grid.RowDefinitions>

      <!-- Title bar -->
      <Grid Grid.Row="0" x:Name="TitleBar" Background="#01000000" Margin="14,0,4,0">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <Ellipse Width="10" Height="10" Fill="{StaticResource Accent}" VerticalAlignment="Center"/>
          <TextBlock Text="grab  ·  Settings"
                     FontFamily="Segoe UI Variable Display, Segoe UI"
                     FontSize="15" FontWeight="SemiBold"
                     Foreground="{StaticResource Text}"
                     Margin="10,0,0,0"
                     VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
          <Button x:Name="MinBtn"   Style="{StaticResource IconBtn}"  Content="&#8211;"/>
          <Button x:Name="CloseBtn" Style="{StaticResource CloseBtn}" Content="&#215;" Margin="4,0,0,0"/>
        </StackPanel>
      </Grid>

      <!-- Body -->
      <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,4,24,0">
        <StackPanel>

          <!-- Downloads section -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="DOWNLOADS"/>

          <TextBlock Style="{StaticResource FieldLabel}" Text="Default download folder"/>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox   Grid.Column="0" x:Name="DownloadFolder" Style="{StaticResource Input}"/>
            <Button    Grid.Column="1" x:Name="BrowseBtn" Style="{StaticResource GhostBtn}" Content="Browse..." Margin="8,0,0,0"/>
          </Grid>

          <CheckBox x:Name="AskBeforeEach" Style="{StaticResource ToggleCheck}"
                    Content="Ask me where to save before each download"
                    Margin="0,16,0,0"/>

          <TextBlock Style="{StaticResource FieldLabel}" Text="Concurrent downloads" Margin="0,20,0,6"/>
          <StackPanel Orientation="Horizontal">
            <Slider x:Name="ConcurrencySlider" Minimum="1" Maximum="6" TickFrequency="1" IsSnapToTickEnabled="True"
                    Width="200" VerticalAlignment="Center"/>
            <TextBlock x:Name="ConcurrencyLabel" Foreground="{StaticResource TextMuted}" FontSize="12"
                       VerticalAlignment="Center" Margin="12,0,0,0" Text="3"/>
          </StackPanel>

          <!-- Cookies section -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="COOKIES"/>
          <TextBlock Style="{StaticResource FieldLabel}"
                     Text="Browser used to read login cookies (needed for private links)"/>
          <ComboBox x:Name="CookieBrowser" Style="{StaticResource DarkCombo}" Width="180" HorizontalAlignment="Left">
            <ComboBoxItem Content="chrome"/>
            <ComboBoxItem Content="firefox"/>
            <ComboBoxItem Content="edge"/>
            <ComboBoxItem Content="brave"/>
            <ComboBoxItem Content="none"/>
          </ComboBox>

          <!-- Notifications section -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="NOTIFICATIONS"/>
          <CheckBox x:Name="ToastsEnabled" Style="{StaticResource ToggleCheck}"
                    Content="Show a Windows toast when a download completes or fails"/>

          <!-- Clipboard section -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="CLIPBOARD"/>
          <CheckBox x:Name="ClipboardWatch" Style="{StaticResource ToggleCheck}"
                    Content="Watch the clipboard -- offer to grab when I copy a URL"/>

          <!-- Safety section -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="SAFETY / PRIVACY"/>
          <TextBlock Style="{StaticResource FieldLabel}"
                     Text="Sensitive downloads route into a hidden .private/ folder (Windows Hidden attribute)."/>
          <CheckBox x:Name="SensitiveByDefault" Style="{StaticResource ToggleCheck}"
                    Content="Treat EVERY download as sensitive"
                    Margin="0,4,0,10"/>
          <TextBlock Style="{StaticResource FieldLabel}"
                     Text="Auto-sensitive URL patterns (one per line -- substring, case-insensitive)"
                     Margin="0,10,0,6"/>
          <Border CornerRadius="6"
                  Background="{StaticResource BgSoft}"
                  BorderBrush="{StaticResource BorderInput}"
                  BorderThickness="1">
            <TextBox x:Name="SensitiveSites"
                     Background="Transparent"
                     Foreground="{StaticResource Text}"
                     BorderThickness="0"
                     FontFamily="Consolas, Segoe UI"
                     FontSize="12"
                     Padding="10,7"
                     Height="110"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
          </Border>
          <TextBlock Foreground="{StaticResource TextDim}" FontSize="10" Margin="4,6,0,0"
                     Text="Example: nhentai.net, allporncomic.com, e-hentai.org"/>

          <!-- Startup section -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="STARTUP"/>
          <CheckBox x:Name="Autostart" Style="{StaticResource ToggleCheck}"
                    Content="Launch grab automatically when Windows starts"/>

          <!-- About -->
          <TextBlock Style="{StaticResource SectionHeader}" Text="ABOUT"/>
          <TextBlock x:Name="VersionLabel" Foreground="{StaticResource TextMuted}" FontSize="12"
                     Text="grab v0.1.0"/>
          <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="OpenStateBtn" Style="{StaticResource GhostBtn}" Content="Open app-data folder"/>
            <Button x:Name="OpenLogsBtn"  Style="{StaticResource GhostBtn}" Content="Open logs" Margin="8,0,0,0"/>
          </StackPanel>

          <Border Height="1" Background="{StaticResource BorderSoft}" Margin="0,26,0,0"/>

          <Button x:Name="ResetBtn" Style="{StaticResource GhostBtn}"
                  Content="Reset all to defaults"
                  HorizontalAlignment="Left"
                  Margin="0,20,0,20"/>
        </StackPanel>
      </ScrollViewer>

      <!-- Action row -->
      <Grid Grid.Row="2" Margin="24,12,24,20">
        <TextBlock x:Name="StatusLine" Foreground="{StaticResource TextDim}" FontSize="11"
                   VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="CancelBtn" Style="{StaticResource GhostBtn}" Content="Cancel" Width="90" Margin="0,0,8,0"/>
          <Button x:Name="SaveBtn"   Style="{StaticResource PrimaryBtn}" Content="Save"   Width="120"/>
        </StackPanel>
      </Grid>
    </Grid>
  </Border>
</Window>

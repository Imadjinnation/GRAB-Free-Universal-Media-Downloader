<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!--
    theme.xaml
    ==========
    Shared arcade design language for GRAB. Every window (popup, settings,
    about) merges this dictionary via <Window.Resources> so no ComboBox,
    Slider, CheckBox, TextBox, Button, ScrollBar, or ContextMenu falls back
    to Windows-native chrome.

    Palette (mockup-matched v0.2.2, warm/CRT phosphor drift):
      Ground   #0A0616   Card     #141024   Card-hi #1F1738
      Accent   #FF2D8C   Cyan     #00E5D2   Amber   #FFD447
      Green    #8DFF6B   Magenta  #FF2E93   Rec     #FF4444
      Text     #F5EBD0   Muted    #8974A6   Dim     #5B547A
      BorderSoft #241A3E (dashed-vibe line)  BorderInput #2A2244

    Fonts (via __GRAB_FONTS__ URI substitution at load time):
      Silkscreen  = wordmark + micro-uppercase only
      VT323       = tabs, labels, kickers, headers, section labels
      Inter       = body text, form inputs, captions
  -->

  <!-- ===================== Palette =====================
       Mockup drift (v0.2.2): the shipped v0.2 palette was too cool. The
       mockup uses warm cream ink, phosphor-teal cyan, yellow-amber, and a
       lime-lean acid green. Border tone shifts up half a step to #241A3E
       so the card edges read as a warmer violet, not indigo. -->
  <SolidColorBrush x:Key="Ground"       Color="#0A0616"/>
  <SolidColorBrush x:Key="Card"         Color="#141024"/>
  <SolidColorBrush x:Key="CardHi"       Color="#1F1738"/>
  <SolidColorBrush x:Key="BorderSoft"   Color="#241A3E"/>
  <SolidColorBrush x:Key="BorderInput"  Color="#2A2244"/>
  <SolidColorBrush x:Key="Text"         Color="#F5EBD0"/>
  <SolidColorBrush x:Key="TextMuted"    Color="#8974A6"/>
  <SolidColorBrush x:Key="TextDim"      Color="#5B547A"/>
  <SolidColorBrush x:Key="Hover"        Color="#241A3E"/>
  <SolidColorBrush x:Key="Accent"       Color="#FF2D8C"/>
  <SolidColorBrush x:Key="Cyan"         Color="#00E5D2"/>
  <SolidColorBrush x:Key="Amber"        Color="#FFD447"/>
  <SolidColorBrush x:Key="Green"        Color="#8DFF6B"/>
  <SolidColorBrush x:Key="Magenta"      Color="#FF2E93"/>
  <SolidColorBrush x:Key="Rec"          Color="#FF4444"/>
  <SolidColorBrush x:Key="SectionLabel" Color="#00E5D2"/>

  <!-- ===================== Text styles ===================== -->
  <Style x:Key="Wordmark" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#Silkscreen"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="22"/>
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
  </Style>

  <Style x:Key="Kicker" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Foreground" Value="{StaticResource Cyan}"/>
  </Style>

  <Style x:Key="SectionHeader" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource SectionLabel}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Margin" Value="0,22,0,8"/>
  </Style>

  <Style x:Key="Body" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#Inter"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="TextWrapping" Value="Wrap"/>
  </Style>

  <Style x:Key="Caption" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="TextWrapping" Value="Wrap"/>
  </Style>

  <!-- ===================== Scrollbar (thin cyan-on-dark, no arrows) ===================== -->
  <Style x:Key="ScrollBarThumbArcade" TargetType="Thumb">
    <Setter Property="OverridesDefaultStyle" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Border CornerRadius="4" Background="#5500E5D2" BorderBrush="#8800E5D2" BorderThickness="1"/>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ScrollBar">
    <Setter Property="Background" Value="#0A0616"/>
    <Setter Property="Foreground" Value="#00E5D2"/>
    <Setter Property="Width" Value="10"/>
    <Setter Property="MinWidth" Value="10"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Grid Background="{TemplateBinding Background}">
            <Track Name="PART_Track" IsDirectionReversed="True">
              <Track.Thumb>
                <Thumb Style="{StaticResource ScrollBarThumbArcade}"/>
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
              </Track.IncreaseRepeatButton>
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
              </Track.DecreaseRepeatButton>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Orientation" Value="Horizontal">
        <Setter Property="Height" Value="10"/>
        <Setter Property="MinHeight" Value="10"/>
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ScrollBar">
              <Grid Background="{TemplateBinding Background}">
                <Track Name="PART_Track" IsDirectionReversed="False">
                  <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumbArcade}"/>
                  </Track.Thumb>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Focusable="False"/>
                  </Track.IncreaseRepeatButton>
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Focusable="False"/>
                  </Track.DecreaseRepeatButton>
                </Track>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- ===================== ArcadeText (TextBox) ===================== -->
  <Style x:Key="ArcadeText" TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource CardHi}"/>
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Cyan}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#Inter"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Padding" Value="10,7"/>
    <Setter Property="Height" Value="32"/>
    <Setter Property="CaretBrush" Value="#FF2D8C"/>
    <Setter Property="SelectionBrush" Value="#FF2D8C"/>
    <Setter Property="SelectionOpacity" Value="0.35"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border x:Name="Bd" CornerRadius="4"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <ScrollViewer x:Name="PART_ContentHost"
                          Margin="{TemplateBinding Padding}"
                          VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadeCombo (ComboBox) ===================== -->
  <ControlTemplate x:Key="ArcadeComboToggleTemplate" TargetType="ToggleButton">
    <Border x:Name="Bd" CornerRadius="4"
            Background="{StaticResource CardHi}"
            BorderBrush="{StaticResource Cyan}"
            BorderThickness="1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="24"/>
        </Grid.ColumnDefinitions>
        <ContentPresenter Grid.Column="0" Margin="10,0,0,0" VerticalAlignment="Center"
                          HorizontalAlignment="Left" RecognizesAccessKey="True"/>
        <TextBlock Grid.Column="1" Text="&#9660;" Foreground="{StaticResource Cyan}"
                   FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
      </Grid>
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="IsKeyboardFocused" Value="True">
        <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
      </Trigger>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="Bd" Property="Background" Value="#241B3E"/>
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <Style x:Key="ArcadeComboItem" TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Padding" Value="10,6"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="#228DFF6B"/>
              <Setter Property="Foreground" Value="{StaticResource Green}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter Property="Foreground" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ArcadeCombo" TargetType="ComboBox">
    <Setter Property="Background" Value="{StaticResource CardHi}"/>
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Cyan}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Height" Value="32"/>
    <Setter Property="Padding" Value="10,4"/>
    <Setter Property="ItemContainerStyle" Value="{StaticResource ArcadeComboItem}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton x:Name="ToggleButton"
                          Template="{StaticResource ArcadeComboToggleTemplate}"
                          Focusable="False"
                          ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                              Margin="10,3,30,3"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Left"/>
            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
              <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                    MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                <Border x:Name="DropDownBorder" Background="{StaticResource Card}"
                        BorderBrush="{StaticResource Cyan}" BorderThickness="1" CornerRadius="4"/>
                <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                  <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                </ScrollViewer>
              </Grid>
            </Popup>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadeSlider ===================== -->
  <Style x:Key="ArcadeSliderThumbStyle" TargetType="Thumb">
    <Setter Property="OverridesDefaultStyle" Value="True"/>
    <Setter Property="Height" Value="14"/>
    <Setter Property="Width" Value="12"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Rectangle x:Name="Th" Fill="{StaticResource Accent}" Width="12" Height="14"
                     Stroke="{StaticResource Accent}" StrokeThickness="0"/>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ArcadeSlider" TargetType="Slider">
    <Setter Property="Focusable" Value="True"/>
    <Setter Property="Foreground" Value="{StaticResource Cyan}"/>
    <Setter Property="Background" Value="{StaticResource CardHi}"/>
    <Setter Property="Height" Value="22"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid x:Name="Root">
            <Border x:Name="TrackBg" Height="6" CornerRadius="0" VerticalAlignment="Center"
                    Background="{StaticResource CardHi}"
                    BorderBrush="{StaticResource BorderInput}" BorderThickness="1"/>
            <TickBar x:Name="TopTicks" Fill="{StaticResource Cyan}" Placement="Top"
                     Height="4" VerticalAlignment="Top" Visibility="Collapsed"/>
            <Track x:Name="PART_Track">
              <Track.Thumb>
                <Thumb Style="{StaticResource ArcadeSliderThumbStyle}"/>
              </Track.Thumb>
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" Focusable="False"/>
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" Focusable="False"/>
              </Track.IncreaseRepeatButton>
            </Track>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="TickPlacement" Value="TopLeft">
              <Setter TargetName="TopTicks" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="TickPlacement" Value="BottomRight">
              <Setter TargetName="TopTicks" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="TrackBg" Property="BorderBrush" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadeCheck (CheckBox) ===================== -->
  <Style x:Key="ArcadeCheck" TargetType="CheckBox">
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#Inter"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <StackPanel Orientation="Horizontal">
            <Border x:Name="Box" Width="16" Height="16" CornerRadius="0"
                    Background="{StaticResource CardHi}"
                    BorderBrush="{StaticResource Cyan}" BorderThickness="1"
                    VerticalAlignment="Center">
              <TextBlock x:Name="Tick" Text="&#9642;" Foreground="{StaticResource Accent}"
                         FontSize="14" FontWeight="Bold"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Visibility="Collapsed"/>
            </Border>
            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Amber}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadePrimary (Button) ===================== -->
  <Style x:Key="ArcadePrimary" TargetType="Button">
    <Setter Property="Foreground" Value="Black"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#Silkscreen"/>
    <Setter Property="FontWeight" Value="Bold"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Height" Value="38"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" CornerRadius="6" Background="{StaticResource Accent}"
                  BorderBrush="{StaticResource Cyan}" BorderThickness="0">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Bd" Property="Opacity" Value="0.35"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadeGhost (Button) ===================== -->
  <Style x:Key="ArcadeGhost" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource Cyan}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Height" Value="32"/>
    <Setter Property="Padding" Value="12,0"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" CornerRadius="6" BorderThickness="1"
                  BorderBrush="{StaticResource Cyan}"
                  Background="Transparent">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Amber}"/>
              <Setter Property="Foreground" Value="{StaticResource Amber}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadeDanger (Button) ===================== -->
  <Style x:Key="ArcadeDanger" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource Rec}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Height" Value="32"/>
    <Setter Property="Padding" Value="12,0"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" CornerRadius="6" BorderThickness="1"
                  BorderBrush="{StaticResource Rec}"
                  Background="Transparent">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource Rec}"/>
              <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ArcadeTab (Button used as tab) =====================
       Mockup active-state (v0.2.2): ALL active tabs use a magenta underline
       #FF2E93 and amber text #FFD447 with a soft amber glow. The old per-tab
       color rule (amber/cyan/green) was mockup-drift and read as inconsistent.
       Non-active tab: transparent underline, muted text.

       The `Tag` attribute values ("active-amber","active-cyan","active-green")
       are kept for backwards compat with popup.ps1's tab-switch dispatcher;
       all three triggers now produce the SAME visual (magenta+amber+glow). -->
  <Style x:Key="ArcadeTab" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="16"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Height" Value="34"/>
    <Setter Property="Padding" Value="14,0"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Grid Background="Transparent">
            <!-- Explicit TextBlock (instead of ContentPresenter) so the amber
                 glow DropShadowEffect can be applied selectively on active. -->
            <TextBlock x:Name="Lbl"
                       Text="{TemplateBinding Content}"
                       Foreground="{TemplateBinding Foreground}"
                       FontFamily="{TemplateBinding FontFamily}"
                       FontSize="{TemplateBinding FontSize}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Margin="{TemplateBinding Padding}"/>
            <Border x:Name="Under" Height="2" VerticalAlignment="Bottom" Margin="8,0" Background="Transparent"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Lbl" Property="Foreground" Value="{StaticResource Text}"/>
            </Trigger>
            <Trigger Property="Tag" Value="active-amber">
              <Setter TargetName="Lbl" Property="Foreground" Value="{StaticResource Amber}"/>
              <Setter TargetName="Lbl" Property="Effect">
                <Setter.Value>
                  <DropShadowEffect Color="#FFD447" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
              </Setter>
              <Setter TargetName="Under" Property="Background" Value="#FF2E93"/>
            </Trigger>
            <Trigger Property="Tag" Value="active-cyan">
              <Setter TargetName="Lbl" Property="Foreground" Value="{StaticResource Amber}"/>
              <Setter TargetName="Lbl" Property="Effect">
                <Setter.Value>
                  <DropShadowEffect Color="#FFD447" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
              </Setter>
              <Setter TargetName="Under" Property="Background" Value="#FF2E93"/>
            </Trigger>
            <Trigger Property="Tag" Value="active-green">
              <Setter TargetName="Lbl" Property="Foreground" Value="{StaticResource Amber}"/>
              <Setter TargetName="Lbl" Property="Effect">
                <Setter.Value>
                  <DropShadowEffect Color="#FFD447" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
              </Setter>
              <Setter TargetName="Under" Property="Background" Value="#FF2E93"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===================== ContextMenu / MenuItem ===================== -->
  <Style TargetType="ContextMenu">
    <Setter Property="Background" Value="{StaticResource Card}"/>
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Cyan}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
  </Style>

  <Style TargetType="MenuItem">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Padding" Value="10,6"/>
    <Style.Triggers>
      <Trigger Property="IsHighlighted" Value="True">
        <Setter Property="Background" Value="{StaticResource Accent}"/>
        <Setter Property="Foreground" Value="White"/>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- ===================== ToolTip ===================== -->
  <!-- Default WPF tooltip is a Windows-yellow popup; override to arcade dark. -->
  <Style TargetType="ToolTip">
    <Setter Property="Background" Value="{StaticResource Card}"/>
    <Setter Property="Foreground" Value="{StaticResource Text}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Cyan}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontFamily" Value="__GRAB_FONTS__#VT323"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Padding" Value="8,4"/>
    <Setter Property="HasDropShadow" Value="False"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToolTip">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Padding="{TemplateBinding Padding}"
                  CornerRadius="3">
            <ContentPresenter/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
